# GitLab service-account personal access token
GITLAB_TOKEN=

# Anthropic API key for the claude CLI (usually already in your shell env)
# ANTHROPIC_API_KEY=

# Optional: path to SSH key for git operations; default uses your agent
# GIT_SSH_COMMAND=ssh -i /path/to/id_ed25519 -o StrictHostKeyChecking=accept-new
